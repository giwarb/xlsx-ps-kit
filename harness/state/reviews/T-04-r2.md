# Review T-04 (round 2)

## 判定: done

## テスト実行
- コマンド: 実行不可。PowerShell 起動前に `codex-windows-sandbox-setup.exe` が見つからず、ローカルコマンド基盤を開始できなかった。
- 結果: 独立実行なし。差分・実ファイルのコード読解で代替した。実装メモには対象テスト passed 15 / failed 0、全回帰 passed 37 / failed 0 とあるが、レビュアー側では未確認。
- EXCEL.EXE 差分: 未取得

## チェックリスト
| # | 項目 | ○/×/- | メモ |
|---|---|---|---|
| C-1 | 関数シグネチャが設計と一致 | ○ | `-Workbook <obj> -Path <string>` で §3.2 と一致。 |
| C-2 | 戻り値の形が設計と一致 | - | 戻り値契約なし。 |
| C-3 | `Test-XlsFormulas` の JSON 契約 | - | 対象外。 |
| C-4 | exit code 契約 | - | 対象外。 |
| C-5 | §0 の確定事項を変更していない | ○ | 製品コードは Excel COM を使用。OOXML 読み取りは保存状態を検証するテスト限定。 |
| C-6 | タスクカードの範囲外を触っていない | ○ | T01 の変更は実装済み関数をスタブ検査から除外するための回帰修正。 |
| S-1 | `GetActiveObject` を使っていない | ○ | 使用なし。 |
| S-2 | COM が `Invoke-XlsSession` 外に漏れていない | ○ | テストの COM 操作はすべて ScriptBlock 内。 |
| S-3 | `finally` で Close→Quit→Release→GC×2 | ○ | 既存の `Invoke-XlsSession` の後始末経路を維持。 |
| S-4 | ScriptBlock 内の例外でもプロセスが残らない | ○ | `Invoke-XlsSession` の `finally` と `AfterEach` の残留検査がある。ただし独立実行は未確認。 |
| S-5 | `Value2` を使い、`Value`/`Text` を数値取得に使わない | ○ | 数値・内容の読み取りは `Value2`。 |
| S-6 | 2次元代入が `[object[,]]` | ○ | round 1 の scalar 代入5箇所はすべて `New-XlsSingleCellArray` 経由に修正済み。 |
| S-7 | `SpecialCells` のヒットなし例外を処理 | - | 対象外。 |
| S-8 | `DisplayAlerts=$false` 中に `Close(true)` しない | ○ | 保存は `Save`/`SaveAs` のみ。既存の Close は `$false`。 |
| S-9 | Excel 全殺しがない | ○ | なし。 |
| S-10 | ReadOnly 検出・STA 検査 | - | T-03 のみ。 |
| P-1 | `#Requires -Version 5.1` | ○ | モジュール・テストともあり。 |
| P-2 | PowerShell 7 専用構文なし | ○ | 該当構文なし。 |
| P-3 | ファイルI/OにUTF-8を明示 | ○ | `StreamReader` に `[Text.Encoding]::UTF8` を明示。 |
| P-4 | Pester 3.4 互換構文 | ○ | `Should Be` 形式で、v5専用構文なし。 |
| T-1 | Pester が緑 | - | サンドボックス環境不備により独立実行できず。実装メモの結果は対象15件・全体37件とも failed 0。 |
| T-2 | `AfterEach` に `Assert-NoOrphanExcel` | ○ | あり。 |
| T-3 | 実 Excel を起動する | ○ | 統合テストは `Invoke-XlsSession` を使用し、モックなし。 |
| T-4 | 受け入れ要点が全部テストにある | ○ | Calculation の焼き込み、再計算、各形式、再保存を検証。.xlsm はタスクカードで許容されたマクロなしの1段目を実施。 |
| T-5 | 異常系が最低1つある | ○ | 存在しない親ディレクトリへの `SaveAs` 失敗を追加し、エラーと状態復元を検証。 |
| Q-1 | コメントベースヘルプがある | ○ | 公開関数と追加ヘルパーにあり。 |
| Q-2 | エラーメッセージが次の一手を示す | ○ | 再計算・保存例外を、パス、親ディレクトリ、権限、ロック、拡張子の確認を促す文に変換。 |
| Q-3 | COM 定数を集約し文書へ追記 | ○ | `$script:Xl` と `com-constants.md` に値・実測記録あり。 |
| Q-4 | 実装メモに COM の罠がある | ○ | Calculation、テンプレート、HasVBProject 等を具体的に記録。 |
| Q-5 | 自己チェックの○が事実と一致 | ○ | round 1 で誤りだった G-08 は修正済み。他項目もコード上は一致。G-15のみレビュアー側で独立実行できていない。 |

## 指摘

新規指摘なし。

round 1 指摘の解消確認:

- `[blocking]` scalar `Value2` 代入: 解消。実行される5箇所すべてが1×1の `[object[,]]` を返すヘルパー経由になっている。
- `[should-fix]` try/finally 構成とエラーメッセージ: 解消。Automatic 切替、再計算、パス判定、保存が同じ `try` に入り、`catch` で行動可能なメッセージへ変換し、`finally` で Manual に戻している。失敗時の検証テストも追加済み。
- `[should-fix]` StreamReader エンコーディング: 解消。UTF-8が明示されている。

## 良かった点（1〜2 行、次のタスクでも続けてほしいこと）

前回指摘を項目ごとに記録し、修正だけでなく異常系テストまで追加できている。Calculation のファイル状態とライブセッション状態を分けた設計意図も、コード・テスト・実装メモで一貫している。

## SKILL.md gotchas 候補（実装メモに書かれていないが気づいた罠があれば）

なし。今回確認した罠は実装メモに記録済み。