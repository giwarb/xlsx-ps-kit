# Review T-03 (round 3)

## 判定: done

## テスト実行

- コマンド: `powershell -NoProfile -ExecutionPolicy Bypass -Command "Invoke-Pester -Path tests\Invoke-XlsSession.Tests.ps1"`（前後に `Get-Process EXCEL -ErrorAction SilentlyContinue | Select Id`）
- 結果: 未実行（passed - / failed -）。Windows サンドボックス初期化用の `codex-windows-sandbox-setup.exe` が見つからず、PowerShell プロセスを起動できなかった。代替として handoff diff と現行コードを読解した。実装メモ記載の T-03 passed 8 / failed 0（2回連続）、T-01 passed 13 / failed 0、T-02 passed 3 / failed 0 は独立確認できていない。静的 fixture `tests/fixtures/blank.xlsx` は存在し、ZIPシグネチャ、中央ディレクトリ、および `[Content_Types].xml`、`xl/workbook.xml`、`xl/worksheets/sheet1.xml` 等の必須構成を確認した。
- EXCEL.EXE 差分: 未確認。前後確認用の `Get-Process` も同じサンドボックス制約で実行できなかった。

## チェックリスト

| # | 項目 | ○/×/- | メモ |
|---|---|---|---|
| C-1 | 関数シグネチャが 01-design.md §3 と一致（引数名・型・既定値） | ○ | `Path`、`ReadOnly`、`Visible`、`ScriptBlock` が設計どおり |
| C-2 | 戻り値の形（オブジェクト／JSON キー名）が設計と一致 | ○ | ScriptBlock の出力をそのまま返す |
| C-3 | `Test-XlsFormulas` の JSON が recalc.py 契約と一致 | - | T-10以降 |
| C-4 | exit code 契約（`error` キーのときだけ非0） | - | T-12 |
| C-5 | 確定事項（§0）に触れる変更がない | ○ | COM方式・依存・公開APIに変更なし |
| C-6 | タスクカードの範囲外を触っていない | ○ | 静的fixture追加とT-01 assertion更新はいずれもT-03のテスト成立・回帰維持に必要な変更 |
| S-1 | `GetActiveObject` を使っていない | ○ | 実行コードは新規Applicationを `Start-XlsApplication` から取得 |
| S-2 | COMが `Invoke-XlsSession` 外に漏れていない（テスト含む） | ○ | fixture作成処理はテストから撤去済み。ロック保持を含むCOM操作は `Invoke-XlsSession` のScriptBlock内のみ |
| S-3 | `finally` で Close→Quit→Release（逆順）→GC×2 | ○ | `Close($false)` → `Quit` → Release(Workbook) → Release(Application) → GC×2 |
| S-4 | ScriptBlock内で `throw` してもプロセスが残らない（テストで確認） | ○ | 異常系テストとfinally経路がある。実行結果は独立確認不可 |
| S-5 | `Value2` を使い、`Value`/`Text` を数値・日付取得に使っていない | - | Range操作なし |
| S-6 | 2次元代入が `[object[,]]` | - | Range操作なし |
| S-7 | `SpecialCells` のヒットなし例外を処理 | - | T-10 |
| S-8 | `DisplayAlerts=$false` 中に `Close(SaveChanges:=$true)` していない | ○ | Closeはすべて `$false`。T-03の実行コード・テストコードに `SaveAs` 呼び出しなし |
| S-9 | 全殺し（`Get-Process EXCEL \| Stop-Process`）がない | ○ | PID＋開始時刻で所有権を照合する既存方式を維持 |
| S-10 | ReadOnly検出・STA検査がある（T-03のみ） | ○ | STAはExcel起動前、ReadOnlyはOpen直後に検査 |
| P-1 | `#Requires -Version 5.1` | ○ | モジュール・テスト双方にあり |
| P-2 | 三項演算子 / `??` / `?.` / `-Parallel` / `$IsWindows` なし | ○ | 5.1非互換構文なし |
| P-3 | ファイルI/Oに `-Encoding UTF8` | ○ | PIDマーカーの `Set-Content` に明示あり |
| P-4 | Pester 3.4で動く構文 | ○ | `Should Be` / `Should Match` 形式。fixtureパスはscriptスコープで定義 |
| T-1 | Pesterが緑（自分で実行） | - | サンドボックス制約により独立実行不可 |
| T-2 | `AfterEach` に `Assert-NoOrphanExcel` | ○ | `tests/Invoke-XlsSession.Tests.ps1:53` |
| T-3 | 実Excelを起動している（モックでない） | ○ | `Invoke-XlsSession` を使う実COM統合テスト |
| T-4 | 02-implementation-plan.md §4の受け入れ要点が全部テストにある | ○ | (a)～(d)に加え、STA、マーカー、明示ReadOnly、Calculationを網羅 |
| T-5 | 異常系が最低1つある | ○ | ファイルロック、ScriptBlock例外、MTAを含む |
| Q-1 | コメントベースヘルプがある | ○ | `.SYNOPSIS`、各 `.PARAMETER`、`.EXAMPLE` あり |
| Q-2 | エラーメッセージが次の一手を示す | ○ | ReadOnly、STA、ファイル不在の各例外が対処を示す |
| Q-3 | COM定数が `$script:Xl` に集約され、`com-constants.md` に追記されている | ○ | 使用中の `xlCalculationManual` のみ集約・確認済み。未使用になった `xlOpenXMLWorkbook` は `$script:Xl` から削除され、参照表も未確認候補へ戻されている |
| Q-4 | 実装メモにCOMの罠が書かれている | ○ | Calculation設定時期、PowerShellホストAPI、未保存Workbook.Path等を具体的に記録 |
| Q-5 | 自己チェック欄の○が事実と一致 | ○ | G-09を含めコード読解上整合。G-15の実行結果のみ独立確認不可 |

## 指摘

なし。round 2 の blocking は解消されている。`New-TestWorkbookFile` と実行可能な `SaveAs` 呼び出しは撤去され、既存ファイルを必要とするテストはコミット済みの静的fixtureを使用している。不要になった `xlOpenXMLWorkbook` も `$script:Xl` から削除されている。

## 良かった点（1〜2 行、次のタスクでも続けてほしいこと）

静的fixture化により、テスト準備のためだけの保存経路と追加COMセッションを除去できています。未使用となった定数を実装表と確認済み記録から戻した点も、G-09とG-12の両方に沿っています。

## SKILL.md gotchas 候補（実装メモに書かれていないが気づいた罠があれば）

なし。