# Review T-12 (round 4)

## 判定: done

round 3 の blocking 1 件は解消済み。`Test-XlsCliOwnershipReady` は `Refresh()` 後の実プロセスについて、`HasExited` と `StartTime.ToUniversalTime().Ticks` を検証しており、PID/StartTicks/runToken の完全一致が成立するまで所有権確立とみなさない。

## テスト実行

- Pester 実行: 環境制約により未実行
- 理由: Pester 3.4 の初期化で `%TEMP%` への `Add-Type` 書き込みが `UnauthorizedAccessException` となる読み取り専用サンドボックスのため
- 結果: passed - / failed -
- EXCEL.EXE 差分: なし（確認前後とも PID なし）
- 静的構文検査:
  - `skill/scripts/Test-XlsFormulas.ps1`: 解析エラー 0
  - `skill/scripts/XlsAgent.psm1`: 解析エラー 0
  - `tests/TestXlsFormulasCli.Tests.ps1`: 解析エラー 0
- 実装者報告: 対象テスト 12/12 を2回、分割回帰 A=43、B=58、C=106、各実行後の EXCEL.EXE 差分ゼロ。独立再実行はできていないため自己申告として参照した。

## チェックリスト

変更のない観点は round 3 の判定を引き継いだ。

| # | 項目 | ○/×/- | メモ |
|---|---|---|---|
| C-1 | シグネチャが設計と一致 | ○ | `<path> [timeoutSec=30] [-Force]` に変更なし。 |
| C-2 | 戻り値・JSON の形が設計と一致 | ○ | 正常系は契約 JSON、例外・timeout は `error` JSON。 |
| C-3 | recalc.py JSON 契約 | ○ | round 3 から変更なし。 |
| C-4 | exit code 契約 | ○ | `error` の場合だけ非 0。 |
| C-5 | 確定事項に変更なし | ○ | 変更なし。 |
| C-6 | タスク範囲外の変更なし | ○ | round 3 指摘への対応範囲内。 |
| S-1 | GetActiveObject なし | ○ | 使用なし。 |
| S-2 | COM が Invoke-XlsSession 外に漏れていない | ○ | 新規テストのフェイクは COM を使用しない。 |
| S-3 | Close→Quit→Release→GC×2 | ○ | 既存 cleanup に加え、強制終了前の所有権を PID/StartTicks/token で照合する。 |
| S-4 | throw 後も残骸なし | ○ | 既存 finally と回収経路を維持。実行確認は環境制約あり。 |
| S-5 | Value2 使用 | - | 該当なし。 |
| S-6 | 2 次元代入が `[object[,]]` | - | 該当なし。 |
| S-7 | SpecialCells ヒットなし処理 | - | 変更なし。 |
| S-8 | 危険な Close なし | - | 新規コードに Close なし。 |
| S-9 | 全殺しなし | ○ | 回収は `Clear-XlsOrphans` のみ。 |
| S-10 | ReadOnly・STA 検査 | - | T-03 専用。 |
| P-1 | `#Requires -Version 5.1` | ○ | 維持されている。 |
| P-2 | PowerShell 7 専用構文なし | ○ | なし。 |
| P-3 | ファイル I/O に UTF8 指定 | ○ | ready・marker・テスト生成物で明示。 |
| P-4 | Pester 3.4 構文 | ○ | `Should Be` 系を使用。 |
| T-1 | Pester が緑 | - | 独立実行不可。実装者報告は 12/12。 |
| T-2 | AfterEach に Assert-NoOrphanExcel | ○ | `tests/TestXlsFormulasCli.Tests.ps1:387-390`。 |
| T-3 | 実 Excel を使用 | ○ | 実 Excel timeout テストを維持。 |
| T-4 | 受け入れ要点を網羅 | ○ | StartTicks 不一致、Action、ready 削除の検証を追加。 |
| T-5 | 異常系が最低1件 | ○ | ファイル不在、起動失敗代替、timeout がある。 |
| Q-1 | コメントベースヘルプ | ○ | 更新済み。 |
| Q-2 | エラーが次の一手を示す | ○ | 変更なし。 |
| Q-3 | COM 定数の集約 | - | 新規 COM 定数なし。 |
| Q-4 | COM の罠を実装メモに記録 | ○ | PID 再利用耐性と ready cleanup を記録。 |
| Q-5 | 自己チェックの○が事実と一致 | ○ | コードから確認可能な項目は一致。G-15 は独立検証できず。 |

## 指摘

### [should-fix] Action 検証を今回作成したマーカーへ明示的に関連付ける

- 場所: `tests/TestXlsFormulasCli.Tests.ps1:519-533,647-696`
- 内容: `Skipped-DeadPid` や安全側 Action を実際に検証するようになり、round 3 より改善している。ただし、結果を今回のフェイク PID／新規マーカーで絞らず、全マーカー中に該当 Action が1件あれば成功する。以前から残っている別マーカーがある環境では、対象マーカーが期待どおり処理されなくても通る余地がある。
- 修正案: `Invoke-XlsFormulasCliProcess` の返却値へ `$proc.Id` を追加し、`MarkerPath` または `Pid` で対象結果を絞ってから、件数1、`Action = 'Skipped-DeadPid'`、対象ファイル削除を検証する。実 Excel 側も `$currentMarkerNamesAtStart - $baselineMarkerNames` と `MarkerPath` を突き合わせる。

blocking ではなく、総合判定には影響しない。

## 良かった点（1〜2 行、次のタスクでも続けてほしいこと）

`Refresh()`、`HasExited`、StartTime Ticks 完全一致を例外時 fail-closed で実装し、round 3 の安全性欠落を正しく解消している。誤った StartTicks を先に提示してから正値へ置換するテストと、HardExit 前の ready 削除も、修正前後を明確に区別できる構成になっている。