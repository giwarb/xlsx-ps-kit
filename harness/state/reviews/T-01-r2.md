# Review T-01 (round 2)

## 判定: done

## テスト実行

- コマンド: `powershell -NoProfile -ExecutionPolicy Bypass -Command Invoke-Pester -Path tests\T01.Tests.ps1`
- 結果: passed 0 / failed 1（実行不可。読み取り専用サンドボックスにより Pester 3.4 の TestDrive ディレクトリ作成が拒否され、Describe 実行前に失敗）
- EXCEL.EXE 差分: なし（実行前後とも PID なし）
- 代替の読み取り専用確認: Pester 3.4.0、モジュールのロード、公開 8 関数、`Set-XlsRange` の3つの ParameterSet、全8スタブの `ThrowStatementAst` を確認

## チェックリスト

| # | 項目 | ○/×/- | メモ |
|---|---|---|---|
| C-1 | 関数シグネチャが 01-design.md §3 と一致（引数名・型・既定値） | ○ | `Set-XlsRange` は `ByData` / `ByCsv` / `ByJson` の3 ParameterSet。各入力元のみ Mandatory |
| C-2 | 戻り値の形（オブジェクト／JSON キー名）が設計と一致 | - | T-01 はスタブのみ |
| C-3 | `Test-XlsFormulas` の JSON が recalc.py 契約と一致 | - | T-10 以降で実装 |
| C-4 | exit code 契約（`error` キーのときだけ非 0） | - | T-12 で実装 |
| C-5 | 確定事項（§0）に触れる変更がない | ○ | COM方式・依存関係等に変更なし |
| C-6 | タスクカードの範囲外を触っていない | ○ | モジュール骨組みと共通テスト基盤の範囲内 |
| S-1 | `GetActiveObject` を使っていない | ○ | COM 呼び出しなし |
| S-2 | COM が `Invoke-XlsSession` 外に漏れていない（テスト含む） | ○ | COM 呼び出しなし |
| S-3 | `finally` で Close→Quit→Release（逆順）→GC×2 | - | T-03 で実装 |
| S-4 | ScriptBlock 内で `throw` してもプロセスが残らない（テストで確認） | - | T-03 の受け入れ項目 |
| S-5 | `Value2` を使い、`Value`/`Text` を数値・日付取得に使っていない | - | COM 読み取り未実装 |
| S-6 | 2 次元代入が `[object[,]]` | - | 代入処理未実装 |
| S-7 | `SpecialCells` のヒットなし例外を処理 | - | T-10 で実装 |
| S-8 | `DisplayAlerts=$false` 中に `Close(SaveChanges:=$true)` していない | - | COM 終了処理未実装 |
| S-9 | 全殺し（`Get-Process EXCEL \| Stop-Process`）がない | ○ | PID 取得のみ |
| S-10 | ReadOnly 検出・STA 検査がある（T-03 のみ） | - | T-01 では該当なし |
| P-1 | `#Requires -Version 5.1` | ○ | モジュール・共通ヘルパー・テストに存在 |
| P-2 | 三項演算子 / `??` / `?.` / `-Parallel` / `$IsWindows` なし | ○ | 該当構文なし |
| P-3 | ファイル I/O に `-Encoding UTF8` | - | テキストファイル I/O なし |
| P-4 | Pester 3.4 で動く構文 | ○ | Pester 3.4.0でテストを解析し Describe 呼び出しまで到達。AST 検査も5.1互換 |
| T-1 | Pester が緑（自分で実行） | - | TestDrive 作成不可のためテストケースを完走できず |
| T-2 | `AfterEach` に `Assert-NoOrphanExcel` | ○ | `tests/T01.Tests.ps1:33` に存在 |
| T-3 | 実 Excel を起動している（モックでない） | - | T-01 は明示的に COM 非対象 |
| T-4 | 02-implementation-plan.md §4 の受け入れ要点が全部テストにある | ○ | 全8スタブについて `ThrowStatementAst` の存在を検査 |
| T-5 | 異常系が最低 1 つある | - | T-01 は COM 非対象 |
| Q-1 | コメントベースヘルプがある | ○ | 公開8関数すべてに存在 |
| Q-2 | エラーメッセージが次の一手を示す | ○ | 各スタブが実装予定タスクを示す |
| Q-3 | COM 定数が `$script:Xl` に集約され、`com-constants.md` に追記されている | - | T-01 は空の器のみ。充填は T-13 |
| Q-4 | 実装メモに COM の罠が書かれている | ○ | COM 未使用。PowerShell 5.1の文字コード・空配列の罠を記録 |
| Q-5 | 自己チェック欄の○が事実と一致 | - | G-05～G-13 は一致。G-15 はサンドボックス制約により独立確認不可 |

## 指摘

なし。

前回の blocking は解消済み。`skill/scripts/XlsAgent.psm1:201`～`:208` の実メタデータでも、各 ParameterSet に入力元が1つだけ存在し、それぞれ Mandatory であることを確認した。

前回の should-fix も解消済み。`tests/T01.Tests.ps1:71`～`:82` は全8関数について `ThrowStatementAst` を検査しており、代替確認でも全件 true だった。

## 良かった点（1〜2 行、次のタスクでも続けてほしいこと）

前回指摘ごとに対応内容と再実行結果が明記され、コードと実装メモが一致しています。ParameterSet と AST を使った契約検査を今後も継続してください。

## SKILL.md gotchas 候補（実装メモに書かれていないが気づいた罠があれば）

なし。