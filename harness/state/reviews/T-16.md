# Review T-16 (round 1)

## 判定: done

## テスト実行
- コマンド: Pester / Excel 実行なし（依頼どおり）。生成スクリプト、モジュール契約、記録表、fixture の Open XML を read-only で静的確認。
- 結果: passed N/A / failed N/A。静的確認では blocking なし。
- EXCEL.EXE 差分: 該当なし（Excel 未起動）
- 補足:
  - `Make-Fixtures.ps1` と `sales.csv` の UTF-8 BOM を確認。CSV はヘッダー + 1,000 行で全行の形式が仕様どおり。
  - 禁止された `GetActiveObject`、COM 直接生成、`Stop-Process`、直接 `Save`/`SaveAs`、`Value2` 直接代入、PowerShell 7 専用構文はなし。
  - fixture 実体を独立確認し、`model.xlsx` 20 数式、`broken.xlsx` 13 数式、`linked.xlsx` 12 外部リンク数式、`macro.xlsm` は macro-enabled 形式かつ VBA part なしで、記録と一致。
  - `model.xlsx` の年見出しは文字列セル、入力セルの青書式、複利計算結果も Open XML 上で確認。

## チェックリスト
| # | 項目 | ○/×/- | メモ |
|---|---|---|---|
| C-1 | 関数シグネチャが設計と一致 | - | 公開関数の変更なし。 |
| C-2 | 戻り値の形が設計と一致 | - | 戻り値契約の変更なし。 |
| C-3 | `Test-XlsFormulas` JSON 契約 | - | 実装変更なし。記録された JSON のキー・状態は既存契約と整合。 |
| C-4 | exit code 契約 | - | 実装変更なし。 |
| C-5 | 確定事項に触れる変更がない | ○ | §0 の方式・依存・公開 API を変更していない。 |
| C-6 | タスクカードの範囲外を触っていない | ○ | fixture、ベンチマーク記録、SKILL/reference の判明した不足、および harness メタデータに限定。 |
| S-1 | `GetActiveObject` を使っていない | ○ | 静的走査で該当なし。 |
| S-2 | COM が `Invoke-XlsSession` 外に漏れていない | ○ | COM を扱うローカルヘルパーは各 `ScriptBlock` 内の呼び出し元からのみ実行される。 |
| S-3 | finally で Close→Quit→Release→GC×2 | ○ | 利用する既存 `Invoke-XlsSession` 実装を静的確認。 |
| S-4 | ScriptBlock 内 throw 時もプロセスが残らない | - | 実行確認は省略。fixture は既存セッション管理に委譲。 |
| S-5 | 数値・日付取得に `Value`/`Text` を使っていない | ○ | 一括データは `Set-XlsRange`、数式は `Formula` を使用。 |
| S-6 | 2 次元代入が `[object[,]]` | ○ | fixture に直接 `Value2` 代入はなく、既存 `Set-XlsRange` の `[object[,]]` 一括代入を使用。 |
| S-7 | `SpecialCells` のヒットなし例外を処理 | - | 対象変更なし。 |
| S-8 | `Close(SaveChanges:=$true)` がない | ○ | 保存はすべて `Save-XlsWorkbook`。 |
| S-9 | Excel の全殺しがない | ○ | PID は比較のみ。`Stop-Process` なし。 |
| S-10 | ReadOnly 検出・STA 検査 | - | T-03 のみ。 |
| P-1 | `#Requires -Version 5.1` | ○ | 先頭にあり、BOM も確認。 |
| P-2 | PowerShell 7 専用構文なし | ○ | 静的走査で該当なし。 |
| P-3 | ファイル I/O に `-Encoding UTF8` | ○ | CSV は `Export-Csv -Encoding UTF8`。実ファイルも BOM 付き。 |
| P-4 | Pester 3.4 互換構文 | - | Pester 変更なし。 |
| T-1 | Pester が緑 | - | 依頼により未実施。 |
| T-2 | `AfterEach` に `Assert-NoOrphanExcel` | - | テスト追加なし。 |
| T-3 | 実 Excel を起動している | ○ | fixture と独立検証はいずれも実 Excel COM を使った記録で、モックではない。 |
| T-4 | 実装計画 §4 の受け入れ要点 | - | ベンチマークタスクのため直接対応なし。B-1〜B-6 の個別基準は記録済み。 |
| T-5 | 異常系が最低 1 つある | - | テスト追加タスクではない。 |
| Q-1 | コメントベースヘルプがある | ○ | 生成スクリプトと主要ローカル関数に記載。 |
| Q-2 | エラーメッセージが次の一手を示す | ○ | 数式永続化失敗と orphan PID を具体的に報告する。 |
| Q-3 | COM 定数が集約されている | - | 新しい Excel 列挙定数の追加なし。色値は既存の実測値を使用。 |
| Q-4 | 実装メモに COM の罠がある | ○ | 数値風文字列の型変換、数式中のシート名正規化、同一セッション保存を詳しく記録。 |
| Q-5 | 自己チェック欄の○が事実と一致 | ○ | 静的確認および記録内容と一致。 |

## 指摘

blocking はありません。

### [should-fix] アポストロフィ方式の説明が実測記録と食い違う
- 場所: `skill/reference/patterns.md:267`
- 内容: 「`NumberFormat='@'` の方が見た目に余計な文字が残らない」とあるが、タスクカードではアポストロフィ方式も、アポストロフィが取り除かれて `Value2` が `"2024"` になると実測している。主たる gotcha と推奨手順は妥当だが、この比較理由だけは裏付けと一致しない。
- 修正案: 「先頭にアポストロフィを付ける方法でも防げる。書式意図を明示できるため、ここでは書き込み前の `NumberFormat='@'` を推奨する」のように、見た目に関する記述を外す。

### [nit] 実装メモ前半に古い反映状況が残っている
- 場所: `harness/state/tasks/T-16.md:81`
- 内容: 前半では patterns.md へ「未反映」「更新は行わない」とある一方、後半では同じ T-16 中に反映したと記録されている。経緯は読み取れるが、最終状態だけを見ると矛盾して見える。
- 修正案: 「この時点では未反映（後述の手順3で反映）」と時点を明記する。

## 良かった点（1〜2 行、次のタスクでも続けてほしいこと）

B-5 の誤った自己報告を独立読取で訂正しており、報告を鵜呑みにしていないことが明確です。A 側未実施、B-5 の縮退、B-6 のプロセス記録依存も隠さず記載され、fixture 生成も主要ガードレールに沿っています。

## SKILL.md gotchas 候補（実装メモに書かれていないが気づいた罠があれば）

なし。編集と保存を同一 `Invoke-XlsSession` に置く注意、および数値風文字列を書き込む前のテキスト書式設定は既に反映されています。